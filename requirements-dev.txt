# Testing
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0
httpx>=0.27          # async test client for FastAPI (TestClient)

# Linting & formatting
ruff>=0.4

# Type checking (optional, CI runs with continue-on-error)
pyright>=1.1

# Mocking
pytest-mock>=3.14

# Useful during dev
ipython>=8.0         # nicer REPL for debugging
